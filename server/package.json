{
  "name": "json-server",
  "version": "1.0.0",
  "description": "A json-server used to mimic a HTTP based rest API for the react internship theoretical training",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "debug": "node  --inspect server.js --watch ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Radu B",
  "license": "ISC",
  "dependencies": {
    "json-server": "file:"
  }
}
